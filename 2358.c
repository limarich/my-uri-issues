#include<stdio.h>
main()
{
	int c, l , i;
	char bacteria[21][100];//10000||21 pois o virus é o ultimo a ser inserido
	scanf("%d", &i);
	for( l = 0 ; l < i ; l++ )
	{	
		scanf("%s", bacteria[l][c]);	

	}
	compara(bacteria);
}
void compara (char vet[21][100])
{
for ( l = 0 ; l < i ; l++ )
{
	for( c = 0 ; c < i ; c++ )
	{
		if(!strcmp(vet[l][100-c]))
	}
	}	
} 
